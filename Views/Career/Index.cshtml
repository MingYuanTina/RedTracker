@model IEnumerable<SurgiNeer.Models.Apply>

@{
    ViewData["Title"] = "Index";
}

@using System.Diagnostics;
@functions{
    public object SendMessage()
    {
        ProcessStartInfo startinfo = new ProcessStartInfo();
        startinfo.FileName = @"C:\Users\tiantian\Desktop\Testing\Testing\bin\Debug\Testing.vshost.exe";
        startinfo.Arguments = "arg1 arg2";
        startinfo.CreateNoWindow = true;
        startinfo.UseShellExecute = false;
        Process myProcess = Process.Start(startinfo);
        return myProcess;
    }
}

<p>
    <img src="~/images/background.jpg" height="100%" width="100%"/>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                First Name
            </th>
            <th>
                Last Name
            </th>
            <th>
                Phone Number
            </th>
            <th>
                Start Date
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
                <div id="historyLog1" style="display:none;">
                    <div>10/16/2016</div>
                    <div>11/18/2016</div>
                    <div>12/20/2016</div>
                </div>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                1234 5678
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a> |
                <button type="button" onclick="DisplayHistory()" >Details</button>
                <button type="button" onclick="PopMessage()">Message</button>
            </td>
        </tr>
}
    </tbody>
</table>
<script>
        function DisplayHistory(firstName)
        {
            if (document.getElementById("historyLog1"))
            {
                document.getElementById("historyLog1").style.display = "block";
            }
            else
            {
                document.getElementById("historyLog1").style.display = "none";
            }

        }

        function PopMessage(){
            alert("Message Sent")
        }
</script>
